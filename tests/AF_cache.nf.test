nextflow_pipeline {

    name "Test Workflow AF_cache.nf"
    script "AF_cache.nf"

    test("Should run without failures") {

        when {
            params {
                // define parameters here. Example: 
                fasta = "${projectDir}/test_data/fasta/all.fasta"
            }
        }

        then {
            assert workflow.success
        }

    }

}
